name: Application CI

on: 
 workflow_dispatch: 
 push: 
  branches: 
   - main  

jobs:
   build: 
    runs-on: Hemants-laptop
    name: Application Build CI
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2
